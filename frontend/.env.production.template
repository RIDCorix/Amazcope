# Environment configuration for SPA deployment
# This file should be created during the build process

# API URL - will be set during deployment
NEXT_PUBLIC_API_URL=__API_URL_PLACEHOLDER__

# App environment
NEXT_PUBLIC_APP_ENV=production

# Enable SPA mode
NEXT_PUBLIC_SPA_MODE=true
